# Core Flask framework
Flask==3.0.3

# Email support
Flask-Mail==0.9.1

# Web server for Render
gunicorn==21.2.0

# Environment variable support
python-dotenv==1.0.1

# Security and encoding helpers
itsdangerous==2.2.0
Werkzeug==3.0.3
Jinja2==3.1.4
MarkupSafe==2.1.5

# Optional: requests library (if any external calls are added in the future)
requests==2.32.3
